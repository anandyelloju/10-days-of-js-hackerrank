// Let and Const

function main() {
    const PI = Math.PI;
    let r = readLine();
    
    // Print the area of the circle:
    console.log(PI * (r * r));
    
    // Print the perimeter of the circle:
    console.log(2 * PI * r);
}